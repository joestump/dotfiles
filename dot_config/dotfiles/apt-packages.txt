# Ubuntu/Debian packages (apt) — the Linux counterpart to ~/.Brewfile.
# One package per line; # comments allowed (inline or full-line). Edit + `chezmoi apply` to install.
#
# vault and gh are installed from their official apt repos by the package script,
# so they don't need to be listed here.
zsh
git
curl
wget
vim
jq
fzf
zoxide
direnv
bats
figlet             # ASCII wordmark for the StumpCloud MOTD
qrencode           # terminal QR for `signal-link` (link a new Signal device)
openjdk-21-jre-headless  # JRE for signal-cli (the Signal MCP daemon backend)
python3-pygments   # provides `pygmentize` — backend for the colorize plugin
# golang-go      # uncomment for the Go toolchain (enables go-tools.txt)
