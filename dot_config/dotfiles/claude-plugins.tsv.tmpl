# Claude Code plugins to ensure installed. One per line:
#   <marketplace-add source>  <plugin>@<marketplace-name>
# Whitespace-separated; # comments allowed. The marketplace source may be a
# GitHub owner/repo, a full git URL (e.g. Gitea), or a local path.
# Add a line + `chezmoi apply` to install it everywhere (the run_after script keeps
# them current). To update one by hand: `claude plugin update <plugin>@<marketplace>`
# (e.g. `claude plugin update qmd@qmd`) — there is no bulk "update all" form.
# GitHub marketplaces add directly by owner/repo. The private Gitea one is a
# LOCAL PATH (chezmoi external clones it to ~/.config/claude-marketplaces/).
tobi/qmd                                       qmd@qmd
{{ .githubUser }}/claude-plugin-sdd                     sdd@claude-plugin-sdd
{{ .githubUser }}/claude-skills                         claude-skills@{{ .githubUser }}
stump-wtf/claude-plugin-switchboard            switchboard@claude-plugin-switchboard
stump-wtf/claude-plugin-cairn                  cairn@claude-plugin-cairn
~/.config/claude-marketplaces/claude-personal  personal@claude-personal
# Full Gitea URL, not stump-wtf/: the GitHub mirror of this repo is still
# EMPTY (never synced), so the plugin only exists on Gitea. Public repo, so
# `claude plugin marketplace add <url>` clones it fine.
https://gitea.stump.rocks/stump.wtf/claude-plugin-harness.git  harness@claude-plugin-harness
# The single agent-skills repo (coding, operating, review families) that
# stumpcloud/skills migrated into on 2026-09-15. Public repo, so the URL form
# works; Gitea rather than the stump-wtf GitHub mirror because that mirror is
# not wired yet (no push-mirror credential, stumpcloud/stumpcloud#298).
# Supersedes the arr-triage / stumpcloud-deploy / proxmox-manage /
# stumpcloud-zfs skills in claude-personal. The retired stumpcloud@stumpcloud-skills
# plugin is uninstalled by run_onchange_after_31-retire-claude-plugins.
https://gitea.stump.rocks/stump.wtf/skills.git  stump-wtf@stump-wtf-skills
