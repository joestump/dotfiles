# Generates ~/.config/chezmoi/chezmoi.toml on `chezmoi init`.
# Tells chezmoi's `vault` template function to shell out to the OpenBao CLI
# (`bao`) instead of `vault`. chezmoi runs: bao kv get -format=json <path>
[vault]
    command = "bao"
